<Project>
  <PropertyGroup>
    <!-- Keep MSBuild intermediates off the exFAT game drive (1 MB clusters waste space on small files). -->
    <BaseIntermediateOutputPath>$(LOCALAPPDATA)\KRILL\obj\$(MSBuildProjectName)\</BaseIntermediateOutputPath>
    <BaseOutputPath>$(LOCALAPPDATA)\KRILL\bin\$(MSBuildProjectName)\</BaseOutputPath>
  </PropertyGroup>
</Project>
